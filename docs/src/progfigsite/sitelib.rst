``progfigsite.sitelib`` Module
==============================

``sitelib`` is an optional subpackage to ``progfigsite``.
It can contain anything you want to reuse in multiple places in your site.
It's common to have a ``constants.py`` or similar file for global constants,
and various modules containing utility functions.
