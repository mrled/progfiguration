"""Inventory storage implementations.

This package contains implementations of the inventory storage interface.
They're designed to be user-extensible,
so that users can write their own inventory storage implementations
if these don't meet their needs.
"""
